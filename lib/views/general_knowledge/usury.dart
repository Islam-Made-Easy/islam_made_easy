import 'package:flutter/material.dart';
import 'package:islam_made_easy/layout/adaptive.dart';

class Usury extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    final isDesktop = isDisplayDesktop(context);
    return Scaffold();
  }
}
